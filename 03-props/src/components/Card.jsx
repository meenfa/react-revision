import React from 'react'

const Card = ({ title, description }) => {
    return (
        <div>
            <h2>{title}</h2>
            <h3>{description}</h3>
            
        </div>
    )
}

export default Card